## 1. Foundation — schemas, registry, core library

- [x] 1.1 Create `data/civic/schemas/` JSON Schema files (civic record, source record, source-registry entry, candidate, research-run manifest) with the closed status vocabulary, cadence set, and domain list from the specs; verify by loading each schema in a smoke check and asserting required fields
- [x] 1.2 Create `data/civic/source-registry.yaml` seeded with research-documented sources only (LGU official site, LGU CIO Facebook page, PSA, PSGC, Comelec, DILG FDPP, BLGF, DPWH, DTI CMCI, DOH, DepEd, BFP, PNP, archived old LGU site), each with stable ID, publisher, URL/discovery, type, collector, cadence, risk tier, access notes, and `evidenceRef` citing the research file that documents it; verify every entry's `evidenceRef` points to an existing `research/` file
- [x] 1.3 Create empty `data/civic/records.json` and `data/civic/sources.json` (schema-valid empty states); verify they parse and satisfy their schemas
- [x] 1.4 Implement `scripts/data/lib/` core: registry loader (minimal fixed-purpose YAML parsing), record/source loaders, atomic write helper (tmp + rename), SHA-256 hashing, stable JSON normalization; verify with `bun test` unit tests covering parse, write-atomicity, and normalization determinism
- [x] 1.5 Implement `scripts/data/validate.ts` covering every validation rule in the civic-data-pipeline spec (unique IDs, closed vocabulary, source references resolve, claimSources point to existing fields, date sanity, no expired `nextReviewOn` on published changing records, cadence/`nextReviewOn` consistency, evidence hash integrity, no secrets/machine-local paths, no duplicates, no blocked-evidence dependence on published records, registry `evidenceRef` resolution); verify with `bun test` fixture cases for each failure mode (a valid fixture passes, each broken fixture fails with the record named)
- [x] 1.6 Add package.json scripts `data:validate`, `data:refresh`, `data:diff`, `data:generate`, `data:report`, and `verify` (typecheck + data:validate + build); verify `bun run data:validate` exits 0 on the empty-but-valid foundation and fails on a deliberately corrupted copy
- [x] 1.7 Add the `bun run data:validate` step to `.github/workflows/ci.yml` between typecheck and build with no network steps; verify CI config changes by running the same command sequence locally on the repo

## 2. Phase-1 canonical domains — officials + emergency contacts

- [x] 2.1 Create source records in `sources.json` for the officials domain from research (`research/government/26-09-city-officials.md`): Comelec-via-Rappler tallies, city CIO Facebook proclamation, LGU site, archived LGU directory; each with retrievedAt, documentType, and verifier; verify `bun run data:validate` resolves every new source and registry cross-reference
- [x] 2.2 Seed canonical official records with stable IDs (`city-mayor-current`, `city-vice-mayor-current`, `district-representative-current`, `sp-member-<n>` / councilor records with a documented ID scheme, `registered-voters-2025`, plus term-history records) from `officials.json` values, with `claimSources`, `riskTier: high`, quarterly cadence, and initial `nextReviewOn`; verify validation passes and every displayed value in the old `officials.json` maps to a canonical record
- [x] 2.3 Create source records + canonical emergency-contact records from `research/emergency/26-09-emergency-hotlines.md` with stable IDs (`emergency-national-911`, `city-hall-trunk-line`, `cdrmo-emergency-contact`, `pnp-sancarlos-contact`, `bfp-sancarlos-contact`, …), marking 2017-archived numbers `needs-reverification` per the historical re-verify note and monthly cadence; verify validation passes and stale high-risk records are flagged, not hidden
- [x] 2.4 Implement `scripts/data/generate.ts` with per-domain emitters; emit `data/officials.json` and `data/emergency-hotlines.json` from canonical records preserving exact existing shapes (mayor/vice_mayor/representative/councilors/history; national/city_hotlines with status+source) and the `_status`/`_source`/`_updated` conventions with registry-derived source references; verify by diffing generated output against the pre-migration files with unchanged facts (differences must be justified source-label changes only) and byte-identical mirrors in `src/data/` + `public/data/`
- [x] 2.5 Document the migration map in `docs/data-pipeline.md` (every `data/*.json` → producer: canonical | legacy-script | manual, with phase targets) and retire `gen-profile-emergency.ps1` for the emergency output only after generated output is verified; verify the map lists all 21 data files and the retired script's responsibility is fully covered by `data:generate`
- [x] 2.6 Run `bun run verify` (typecheck + validate + build) and verify the site renders identically for officials/emergency data (spot-check `/government`, `/government/officials`, `/contact`)

## 3. Research runs, diff, refresh, promote

- [x] 3.1 Implement the research-run writer/reader in `lib/` (manifest with per-source entries and outcomes, candidates, conflicts, findings, evidence dir, run-ID dating with `-n` suffixing); verify a fixture run round-trips and `validate` accepts it while refusing a manifest with unknown outcome values
- [x] 3.2 Implement `scripts/data/diff.ts`: stable normalization, per-record outcomes (UNCHANGED/NEW/CHANGED/MISSING/STALE/CONFLICT/SOURCE_UNAVAILABLE/SOURCE_CHANGED), markdown review report (OLD/CANDIDATE/SOURCE/RESULT/ACTION per record), exit codes; verify with `bun test` scenarios A (unchanged → no rewrite), B (changed → candidate, canonical untouched), C (source unavailable → no deadline change), D (conflict → blocked candidate)
- [x] 3.3 Implement fixture-driven collectors `scripts/data/collectors/facebook.ts` and `city-website.ts` plus shared `parsers/json.ts`/`parsers/html.ts` (deterministic: same evidence → same candidates, `provisional` status, polite-fetch config, registry-driven only, unregistered source refused); verify with recorded fixture evidence that two runs produce equivalent candidates and network refusal behavior works
- [x] 3.4 Implement `scripts/data/refresh.ts` orchestration (due-source selection from registry cadence + `--source`/`--domain` filters, run creation, collector dispatch, failure recording without canonical mutation, interrupt safety — canonical files untouched when aborting mid-run); verify Scenario A–D end-to-end dry runs on fixtures with `bun run data:refresh -- --source=fixture` style tests
- [x] 3.5 Implement `scripts/data/promote.ts` (explicit reviewer acceptance: append history revision, set acceptedBy/acceptedAt/lastVerified, recompute nextReviewOn from cadence, provisional→verified; refuse high-risk promotion when reviewer equals collector; refuse conflict-blocked candidates; news auto-path allowed only as status `reported`); verify with `bun test` that self-verification and conflict promotion are refused and that history preserves the previous revision (Scenario F)
- [x] 3.6 Implement `scripts/data/report.ts` (staleness, conflicts, coverage gaps, source health) producing a markdown summary; verify it reports a deliberately stale fixture record and that `bun run data:report` runs offline
- [x] 3.7 Implement validator check: no pipeline command modifies `research/` outside `research/runs/`; verify with a fixture run asserting topic-file hashes are unchanged

## 4. Facebook ingestion re-route

- [x] 4.1 Refactor `sync-facebook.js` reusable logic (categorize, deriveTitle, truncate, toDate, transformPost, isValidItem, merge logic) into the pipeline lib and re-target ingestion to produce research-run candidates for the news domain; preserve retry/backoff, fixture mode, dormant-without-token, atomic writes, and retain-on-failure behavior; verify with a Graph-API fixture producing a run + candidates and no direct `data/news.json` write
- [x] 4.2 Add the justified low-risk auto-path: official-page news candidates may be auto-promoted as `reported` (never `verified`, never high-risk categories); verify with `bun test` that a news auto-promotion keeps status `reported` and that non-news records cannot use it
- [x] 4.3 Implement the news generator emitter: `data/news.json` (exact shape incl. `recency`, manual+fb merge order) from canonical news records; seed existing manual entries as accepted records; verify before/after diff on unchanged content is empty, `/news` and `/admin/news-editor` still work against the generated file, and mirrors stay byte-identical
- [x] 4.4 Correct stale documentation in `sync-facebook.js` header (nonexistent `facebook-sync.yml` workflow and `assets/js/news.js` references) to describe the actual pipeline flow; retire `gen-news.ps1` after parity verification; verify grep shows no stale workflow/asset references remain

## 5. Remaining legacy script domains

- [x] 5.1 Seed canonical records + sources for the demographics domain (census history, barangay populations, households, profile facts) from `gen-demographics.ps1` facts with research-cited sources; emit `demographics.json` + `city-profile.json` via generate; verify parity diff vs pre-migration files and mirror integrity, then retire the demographics/profile responsibility of `gen-profile-emergency.ps1`/`gen-demographics.ps1` per the map
- [x] 5.2 Repeat seeding + generation + verified retirement for fiscal/CMCI (`gen-fiscal-cmci.ps1` → `fiscal_transparency.json`, `competitive-index.json`) with per-document cadence and research-documented source records; verify parity diff and validation
- [x] 5.3 Repeat seeding + generation + verified retirement for legislative (`gen-legislative.ps1` → `ordinances.json`, `resolutions.json`) and barangay (`gen-barangays.ps1` → barangay data inputs used by `src/data/`) with quarterly + post-BSKE event-driven cadences; verify parity diff and that all `gen-*.ps1` producers are retired or documented as intentionally retained
- [x] 5.4 Verify every remaining manually-maintained file (services, schools, tourism, etc.) is documented in the migration map with its state and (where applicable) a follow-up note; verify `bun run verify` passes and the full frontend spot-check renders correctly

## 6. Runbook, scheduled refresh, acceptance

- [x] 6.1 Write the agent refresh runbook (AGENTS.md data-pipeline section + `docs/data-pipeline.md`): commands, order, research-run requirements, never-overwrite rules, promotion rules, conflict handling; verify a fresh agent can follow it to run a fixture refresh without other context (Scenario H dry run)
- [x] 6.2 Add `.github/workflows/refresh.yml` (phase 6): scheduled/manual trigger → `data:refresh --due` → `data:diff` → open/update PR with run artifacts via `gh`; never auto-merge, no secrets in data; verify the workflow file is valid (`gh workflow list`/actionlint-equivalent check) and a manual dispatch on a fixture source produces a PR with no canonical changes on `main` (Scenario: scheduled refresh is reviewable)
- [x] 6.3 Execute the full acceptance matrix against fixtures/real data: A (unchanged), B (changed), C (source unavailable), D (conflict), E (stale detection), F (supersession preserves history), G (frontend compatibility — `bun run verify` + spot-check pages), H (fresh-agent runbook run); verify each scenario's expected result and record outcomes in the change's checklist before requesting verification

## Acceptance outcomes (recorded 2026-09-14, all passing)

- A (unchanged): `refresh.test.ts` scenario A + `diff.test.ts` scenario A — UNCHANGED, canonical byte-identical.
- B (changed): `refresh.test.ts` scenario B + `diff.test.ts` scenario B — CHANGED candidate, canonical untouched.
- C (source unavailable): `refresh.test.ts` scenario C (refused endpoint → `unavailable`, 0 candidates, canonical untouched); live CI dispatch recorded `collected`/`failed` honestly.
- D (conflict): `refresh.test.ts` scenario D + `diff.test.ts` scenario D (CONFLICT) + `promote.test.ts` refuses conflicting candidates.
- E (stale detection): `diff.test.ts` stale flag + `validate.test.ts` stale failure + `report.test.ts` stale fixture; live `data:report` shows Stale: 0.
- F (supersession preserves history): `promote.test.ts` keeps the previous revision in `history`.
- G (frontend compatibility): `bun run verify` green (tsc + validate 0 errors + 148-page build); `out/` spot-checks hit on statistics, budget, officials, about, Turac barangay page, and the news/ordinance static payloads.
- H (fresh-agent runbook run): Scenario H commands ran verbatim — offline refresh records `skipped` with a complete run dir, diff/report/validate all offline-clean.
- Scheduled refresh reviewable: 8 live dispatches of `refresh.yml` — refresh/diff/validate/guard green on runners; PR #73 (closed after verification) carried only `research/runs/` artifacts, no auto-merge, merge blocked pending review, `main` untouched. Two fixes landed from the exercise: YAML literal-block indentation (flush-left lines kill trigger parsing) and collision-free run ids for same-day reruns. One owner action remains: enable Settings → Actions → General → "Allow GitHub Actions to create and approve pull requests" so the workflow's `gh pr create` succeeds (verification PR was opened with a user token instead).
- Suite totals: `bun test` 89 pass / 0 fail (13 files); `tsc --noEmit` clean.
